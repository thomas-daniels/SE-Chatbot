machine:
  python:
    version: 3.4
dependencies:
  post:
    - git submodule update --init
    - pip install pytest bs4 websocket-client
    - pip install requests --upgrade
test:
  override:
    - python -m pytest tests
